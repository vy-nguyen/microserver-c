
#include <db/model/tag_attr.h>

/*
 *
try {
        // Connect to the MySQL database
        //         soci::session sql(soci::mysql, "db=your_database user=your_user password=your_password host=localhost");
        //
        //                 // Create an example person
        //                         Person new_person = {0, "John Doe", 30};
        //
        //                                 // Insert the person into the database
        //                                         sql << "INSERT INTO person (name, age) VALUES (:name, :age)", soci::use(new_person);
        //
        //                                                 // Retrieve a person from the database by name
        //                                                         Person fetched_person;
        //                                                                 sql << "SELECT id, name, age FROM person WHERE name = :name", soci::into(fetched_person), soci::use(new_person.name);
        //
        //                                                                         // Output the fetched person’s details
        //                                                                                 std::cout << "Fetched Person - ID: " << fetched_person.id << ", Name: " << fetched_person.name << ", Age: " << fetched_person.age << std::endl;
        //
        //                                                                                     } catch (const soci::soci_error& e) {
        //                                                                                             std::cerr << "SOCI error: " << e.what() << std::endl;
        //                                                                                                     return 1;
        //                                                                                                         }
*/
